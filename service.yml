# bucket service definition draft
service:
  port: 8093
  volumes:
    - ./data/prod:/app/data
  healthcheck:
    path: /api/artifact/count
    interval: 60s
    timeout: 5s
    retries: 3
    start_period: 10s