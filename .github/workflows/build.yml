name: Build and Push Image

on:
  push:
    branches:
      - main
      - dev

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: image=moby/buildkit:latest
          install: true
          network: host
          buildkitd-config-inline: |
            [registry."100.126.211.77:3002"]
              http = true
              insecure = true

      - name: Login to Gitea Registry
        run: |
          echo "${{ secrets.CI_TOKEN }}" | docker login \
            100.126.211.77:3002 \
            -u ${{ gitea.actor }} \
            --password-stdin

      - name: Restore .env
        run: |
          echo "${{ secrets.ENV_FILE_B64 }}" | base64 -d > .env
          ls -l .env

      - name: Build Image
        run: |
          docker build -t 100.126.211.77:3002/${{ gitea.repository }}:latest .

      - name: Push Image
        run: |
          docker push 100.126.211.77:3002/${{ gitea.repository }}:latest
